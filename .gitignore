# Nuxt
.nuxt
.output
.data
.env
.env.*

# Node
node_modules
npm-debug.log*
pnpm-debug.log*

# Build
dist

# OS
.DS_Store

# Test/dev artefacts
.playwright-mcp
*.png

# Large OSM data files
*.osm
*.osm.pbf
*.zip

# OSM2World tooling (JARs, runtime deps)
*.jar
lib/
osm2world.sh
osm2world-windows.bat
standard.properties

# Large 3D assets — too big for git (GLTF city model + textures are 450MB+)
# Host these separately (S3, R2, etc.) and reference via URL or .env
public/gltf/

# OSM2World texture/resource libraries (350MB+)
textures/
resources/
